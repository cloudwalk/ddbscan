""" Discrete DBSCAN implementation
"""

# Import our package only class, so that other can access it with:
#   import ddbscan
#   ddbscan.DDBSCAN
# instead of the valid, but more cumbersome
#   from ddbscan import ddbscan
#   ddbscan.DDBSCAN
from .ddbscan import DDBSCAN

